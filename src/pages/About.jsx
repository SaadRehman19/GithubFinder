const About = () => {
    return ( <
        div className = "text-6xl container mx-auto" >
        <
        h3 > Github Finder < /h3> <br / >
        <
        p > Hello this is simply an about page < /p> { /* {process.env.REACT_APP_GITHUB_URL} */ } <
        /div>
    );
};

export default About;